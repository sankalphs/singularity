{
  "name": "singularity2-server",
  "version": "1.0.0",
  "private": true,
  "description": "SINGULARITY SpacetimeDB server module",
  "scripts": {
    "build": "spacetime build",
    "publish": "spacetime publish --delete-data=never --yes"
  },
  "dependencies": {
    "spacetimedb": "2.10.*"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}
